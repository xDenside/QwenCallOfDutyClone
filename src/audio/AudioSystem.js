// STUB — replaced by the Audio agent. Contract in /CONTRACT.md.
export class AudioSystem {
  constructor(game) { this.game = game; }
  init(game) {}
  play(name, opts) {}
  setListenerFromCamera(camera) {}
  update(dt, game) {}
}
