// STUB — replaced by the AI agent. Contract in /CONTRACT.md.
export class EnemySystem {
  constructor(game) { this.game = game; this.npcs = []; }
  init(game) {}
  raycastTargets(origin, dir, maxDist) { return null; }
  damage(npc, amount, point, opts) {}
  update(dt, game) {}
}
