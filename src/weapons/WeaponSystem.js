// STUB — replaced by the Weapons agent. Contract in /CONTRACT.md.
export class WeaponSystem {
  constructor(game) {
    this.game = game;
    this.current = { name: 'M4A1', adsFov: 55 };
  }
  init(game) {}
  debugFire() {}
  update(dt, game) {}
}
