// STUB — replaced by the FX agent. Contract in /CONTRACT.md.
export class EffectsSystem {
  constructor(game) { this.game = game; }
  init(game) {}
  muzzleFlash(pos, dir, weaponName) {}
  tracer(from, to, opts) {}
  impact(point, normal, material, opts) {}
  explosion(point, opts) {}
  shell(pos, dir) {}
  blood(point, dir) {}
  update(dt, game) {}
}
